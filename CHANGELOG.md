# 更新日志 (Changelog)

本项目所有值得记录的变更均会收录于此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本 (Semantic Versioning)](https://semver.org/lang/zh-CN/)。

## [Unreleased]

暂无待发布变更。

## [v0.5.1] - 2026-08-10

### 新增

- **会期时间流动**：学术组可在会期管理页设置「当前会期时空」（会期虚拟时间）与时间流动比（如 `1:480` = 现实 1 分钟对应会期 480 分钟），并可随时**暂停 / 恢复**时间流动；暂停时冻结在暂停时刻，恢复后从冻结值继续流动
- **顶部时间条栏（全角色可见）**：所有页面顶栏实时展示会期时间（每 2 秒本地推算更新），流动比大于 1 时附带 `1:N` 标识、暂停时显示「已暂停」状态；学术组任何设置变更经 SSE 实时同步到所有在线端，代表端只读展示

### 其他

- `global_state` 表新增 `simTimeBase` / `baseRealTime` / `flowRatio` / `isRunning` 4 个字段，由 `synchronize` 自动同步
- 新增仅学术组可用的 3 个 API：`PUT /api/periods/time`（设置时间与流动比并开始流动）、`PUT /api/periods/time/pause`（暂停）、`PUT /api/periods/time/resume`（恢复）
- `GET /api/periods/current` 返回结构扩展：新增 `clock` 字段（全局时钟状态）
- 前端构建产物随版本重建

### 升级说明

直接拉取最新代码（或覆盖 `dist/` 与 `frontend/dist/`）后重启服务即可。数据库自动建表，无需手动迁移；升级前建议备份 `dev.db`。

## [v0.5.0] - 2026-08-10

### 新增

- **会期管理**：全局当前会期状态（学术组切换、代表只读展示），支持下拉切换 /「下一个会期」自动递增 / 手动输入编号（名称选填），切换即时生效；作为危机时间线与指令提交的归属依据
- **危机时间线**：学术组发布局势更新与新闻（自动编号 `会期.序号`，局势更新与新闻各自独立编号、删除不回填；内容与附件至少其一，图片内联展示），代表按会期 / 类型组合筛选查看；SSE 实时推送 + 浏览器通知
- **指令提交**：代表按指令类型提交指令（自动编号 `[内阁名][指令类型][会期.序号]`，内容必填、附件可选、等待中可删除且编号不回填），学术组审核（接受 / 驳回不可逆 + 可选文本 / 图片 / 文件答复），管理员管理指令类型（6 种预设 + 自定义增删；删除后已提交指令保留类型名称快照）
- **不对称消息**：学术组与单个内阁之间的一对一私密沟通通道（功能与磋商空间一致：文字、图片内联、粘贴即上传、附件下载），同内阁成员共享对话，管理员不可见
- **指令审核内阁筛选**：学术组审核指令时可按会期 / 指令类型 / 内阁组合筛选

### 其他

- 新增 6 张表（`conference_periods`、`global_state`、`timeline_entries`、`directive_types`、`directives`、`asym_messages`），由 `synchronize` 自动同步；`files` 表 `SpaceType` 新增 `TIMELINE` / `DIRECTIVE` / `ASYMMETRIC`
- 共享上传 / 下载工具抽取至 `src/common`（`createUploadOptions` / `setContentDisposition` / `getMimeType`）
- 前端构建产物随版本重建

### 升级说明

直接拉取最新代码（或覆盖 `dist/` 与 `frontend/dist/`）后重启服务即可。数据库自动建表，无需手动迁移；升级前建议备份 `dev.db`。

## [v0.4.0] - 2026-08-09

### 新增

- **浏览器通知提醒（对代表）**：公共空间新增文件、所在磋商群聊有新消息时，向代表推送浏览器通知。功能默认开启，进入系统时自动请求通知权限；若无法获取权限，弹窗提醒（含浏览器设置指引，支持勾选「不再提醒」）
- **主席团（学术组）收件**：代表向会议空间提交文件时，学术组全员收到通知提醒；学术组不接收磋商空间消息通知
- **通知设置**：头部用户菜单新增「通知设置」弹窗，可全局开启/关闭浏览器通知，并查看当前浏览器授权状态（未授权时提供重新请求入口）
- **群聊免打扰**：磋商空间每个会话列表项新增铃铛按钮，可对特定群聊静音；当前正在查看的群聊不弹通知
- **逐条推送 + 冷却节流**：磋商消息逐条独立推送（不再 15 秒合并），每个群聊连续推送 2 条后进入 15 秒冷却期，避免刷屏；冷却期间消息不推送，应用内未读角标正常更新
- **学术团队通知总控页**：学术组侧边栏新增「通知总控」，只读查看各代表的通知开关状态、浏览器权限状态（最近上报，仅供参考）与实时连接状态（在线 / 轮询回退 / 离线），支持按姓名/内阁搜索
- **轮询模式通知兜底**：SSE 失联降级为轮询时，磋商消息通知通过会话未读增量检测继续推送（15 秒检测周期）；页面恢复可见时立即补检测，规避后台标签页计时器节流造成的通知滞后
- **SSE 事件协议扩展**：`file.changed` 事件携带文件名、`message.new` 事件携带发送方内阁 ID（向后兼容，仅新增可选字段）
- **SSE 活跃连接追踪**：服务端维护实时连接计数（多标签页引用计数），支撑总控页连接状态展示

### 修复

- **修复通知设置懒创建的并发竞态**：前端登录时 `getSettings` / `reportConnectionMode` / `reportPermission` 几乎同时发起，多个请求同时发现设置行不存在并同时插入，触发 SQLite `UNIQUE constraint failed: user_notification_settings.userId` 错误。改用 `INSERT OR IGNORE` 原子创建，并发冲突时静默跳过
- **修复群聊免打扰并发双插**：`user_session_dnd` 表唯一约束在并发请求下可能冲突，同样以 `INSERT OR IGNORE` 处理

### 其他

- 数据库新增 `user_notification_settings`（用户通知设置，1:1）与 `user_session_dnd`（群聊免打扰）两张表，由 `synchronize` 自动同步
- 前端构建产物随版本重建

### 升级说明

直接拉取最新代码（或覆盖 `dist/` 与 `frontend/dist/`）后重启服务即可。数据库自动建表，无需手动迁移；升级前建议备份 `dev.db`。

**注意：** 浏览器通知（Web Notification API）仅在 HTTPS 或 `localhost` 环境可用，局域网 IP 的 http 部署无法收到通知；长时间切走的后台标签页可能被浏览器休眠（Edge 睡眠标签页 / Chrome 内存节省程序），导致通知暂停，可在浏览器设置中将本站加入例外，或保持标签页处于前台。

## [v0.3.0] - 2026-08-08

### 新增

- **移动端响应式适配**：小屏（<768px）下侧边栏折叠为抽屉菜单、文件列表改为卡片式展示、磋商空间会话列表变为覆盖式抽屉、弹窗与登录/注册表单自适应宽度，手机浏览器即可顺畅使用全部功能
- **从内阁复制文件到群聊**：代表可在磋商空间选择本内阁文件，一键复制为群聊文件消息（物理复制，等效下载再上传，独立文件记录，不占额外引用关系）
- **磋商文件独立空间**：磋商附件改用独立 `CONSULT` 空间类型存储，不再混入发送者内阁空间列表；磋商文件仅能通过会话消息下载接口访问，杜绝经文件空间接口越权下载

### 修复

- **修复删除文件 500 错误**：删除被群聊消息引用的文件时触发 SQLite 外键约束失败（`FOREIGN KEY constraint failed`），现于删除前先解除相关消息的文件引用（消息文本保留，文件记录正常删除）
- **修复磋商会话列表在移动端无法关闭**：移动端打开会话列表覆盖层后，点击右侧空白区域即可关闭

### 其他

- 前端构建产物随版本重建（`frontend/dist/` 已包含移动端适配）

### 升级说明

直接拉取最新代码或覆盖 `dist/` 与 `frontend/dist/` 后重启服务即可。数据库表结构变更（`files.spaceType` 新增 `CONSULT` 枚举）由 `synchronize` 自动同步，无需手动迁移；升级前建议备份 `dev.db`。

## [v0.2.2] - 2026-08-08

### 新增

- **磋商空间文字消息聊天**：内阁之间除文件往来外，现在可以直接发送纯文字消息（Enter 发送 / Shift+Enter 换行，中文输入法友好）
- **消息发送人展示**：所有消息与文件统一显示发送人「用户名（内阁名）」，学术组消息显示「用户名（学术）」
- **图片内联预览**：图片文件在聊天中直接预览缩略图，点击放大查看（支持缩放/旋转），无需下载
- **粘贴即上传**：在聊天输入框聚焦时直接粘贴图片或文件，先预览确认（缩略图/文件名/大小/可选附言）再发送
- **文件+附言**：发送文件时可同时附带一句说明文字

### 变更

- **群聊解散/退出**：新增群聊解散（学术组）与退出（代表）功能。代表退出保留群聊供学术组审议；学术组解散时彻底清理消息与文件
- **内阁删除行为调整**：删除内阁不再清除其参与的群聊与磋商文件，保留供学术组审议；仅清理该内阁空间文件与成员关系
- **管理员群聊权限**：管理员获得与学术组一致的群聊查看与解散权限
- **下载响应优化**：按文件扩展名设置正确的 Content-Type 响应头

### 修复

- **修复 SQLite 外键约束导致的群聊/内阁级联清理失败**：先删消息再删文件
- **修复代表退出群聊时解散会话的误触发**：退出不再解散群聊

### 升级说明

直接拉取最新代码（或覆盖 `dist/` 产物）后重启服务即可。数据库通过 `synchronize: true` 自动迁移 messages 表（`fileId` 可空、新增 `content` 与 `senderUserId` 列），无需手动迁移。使用 MariaDB 生产库的部署同样自动迁移。

## [v0.2.1] - 2026-08-08

### 修复

- **iOS 注册/登录输入丢失**：苹果设备（Safari 及微信内置浏览器）上，输入用户名后按键盘 return，输入内容全部消失。根因有二：表单内缺少 `type="submit"` 提交按钮，iOS 按 return 时触发隐式提交（WebKit 已知行为）直接刷新页面；输入框缺少 `name` / `autocomplete` 属性，iOS 密码自动填充启发式将注册表单误判为登录表单，干扰并覆盖用户输入
  - 修复内容：注册页用户名 `autocomplete="username"`、密码与确认密码 `autocomplete="new-password"`；登录页密码 `autocomplete="current-password"`；登录/注册提交按钮改为原生 submit 类型，键盘 return 走正常表单事件，不再刷新页面；移除登录按钮重复处理，避免 return 触发双重提交
- **登录页输入框图标重复**：用户名输入框的 prefix-icon 与 #prefix 插槽同时渲染，显示两个相同图标，已修复
- **页面语言声明修正**：`<html lang="en">` → `zh-CN`

### 其他

- 许可证声明统一为 GPL-3.0-or-later
- 镜像同步 workflow 修正写权限并更新

### 升级说明

直接拉取最新代码或覆盖 `frontend/dist/` 后重启服务即可，无需数据库迁移。

## [v0.2.0] - 2026-08-07

### 新增

- **SSE 实时通知系统**：新增 Events 模块，当文件、会议（Session）、内阁（Cabinet）发生变更时，系统通过 SSE（Server-Sent Events）向相关用户广播实时通知
- **前端 SSE 客户端 + 轮询降级**：文件空间页面接入 SSE 客户端，支持自动刷新；在不支持 SSE 的环境下自动降级为轮询模式，保证功能可用性
- **Ticket 鉴权机制**：为 SSE 连接增加 ticket 鉴权，确保连接安全

### 修复

- **修复登录响应缺失 cabinetId 的问题**：此前登录接口返回的 user 对象中缺少 cabinetId 字段，导致代表创建磋商群组和内阁信息自动刷新功能异常，现已修复

### 其他

- 重新构建后端 dist 产物，集成 SSE events 模块
- 重新构建前端 dist 产物，集成 SSE 客户端
- 如果用户直接使用 `dist/` 目录部署，本次升级需要重新拉取构建产物或重新执行 `npm run build`

## [v0.1.3] - 2026-08-06

### 修复

- **修复文件下载"物理文件不存在"错误**：公共空间和会议空间上传的文件，下载时一律报错
  "物理文件不存在"。原因为数据库存储路径与实际物理存储位置不一致——存储路径中多了一层
  `PUBLIC` / `CONFERENCE` 子目录，而物理文件实际直接存放于 `uploads/public/`、
  `uploads/conference/` 下。现已将存储路径修正为与实际位置完全一致，内阁空间文件不受影响。

### 其他

- 使用当前 TypeScript 版本重新构建 `dist/` 产物（无运行逻辑变更）。

### 升级说明

直接拉取最新代码或覆盖 `dist/` 后重启服务即可，无需迁移数据库。

## [v0.1.2] - 2026-08-06

### 新增

- 新增"关于"页面：免责声明、GPL v3 完整许可证查看与 GitHub 链接。
- 前端构建产物纳入 git 版本管理，支持单进程直接部署。

### 变更

- 全部源码文件补充 GPL v3 许可证头。

## [v0.1.1] - 2026-08-05

### 新增

- 初始版本发布。
- 四大空间文件管理：内阁空间（内部隔离）、公共空间（官方发布）、会议空间（提交与审核）、磋商空间（群组文件往来）。
- 拉群磋商：代表可创建包含 2 个及以上内阁的群组进行文件磋商，学术组可查看全部群聊并参与指导。
- 管理员系统：账户管理（添加/改密/删除）、内阁管理（开设/删除，级联清理文件）。
- 基于 JWT 的身份鉴权与权限隔离。
- 中文文件名上传/下载支持（RFC 5987 响应头 + UTF-8 解析）。
- SQLite（本地开发）↔ MariaDB（生产）双数据库支持。

[Unreleased]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.5.0...HEAD
[v0.5.0]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.4.0...v0.5.0
[v0.4.0]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.3.0...v0.4.0
[v0.3.0]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.2.2...v0.3.0
[v0.2.2]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.2.1...v0.2.2
[v0.2.1]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.2.0...v0.2.1
[v0.2.0]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.1.3...v0.2.0
[v0.1.3]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.1.2...v0.1.3
[v0.1.2]: https://git.ssug.top/xiqiu/MUNFMS/compare/v0.1.1...v0.1.2
[v0.1.1]: https://git.ssug.top/xiqiu/MUNFMS/releases/tag/v0.1.1
